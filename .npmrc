# Force pnpm usage only
engine-strict=true
package-manager=pnpm

# Prevent npm from creating package-lock.json
package-lock=false

# Auto install peer dependencies
auto-install-peers=true

# Use exact versions for better reproducibility
save-exact=true
